# TurboVLA 活动回放数据

生成时刻：2026-09-14 00:50:25。

本目录包含一次真实 forward 的 trace、量化 payload、descriptor、指令、周期／流量明细、activity events、RTL 校验和 Vivado 汇总。大文件保留原始 SHA256；`trace_manifest.json` 是当前目录的机器可读清单。payload 只用于回放和核对，不在 HTML 中内嵌。
