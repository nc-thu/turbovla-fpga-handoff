# TurboVLA 十项优化：输入说明

完成时刻：2026-09-14 02:03:37。

本目录是本轮算法侧的记录目录。它没有重新采集 GPU，也没有改模型和量化代码。输入来自只读的 `data/v8_2026-09-13_231621/raw_capture/`，其中保存了一个真实 `libero_spatial` Spatial task 0 的完整 forward。

本轮把导航中已经做过、或者可以落到当前 16×48 阵列的十个点列出来。真正减少周期的是读／算／后处理重叠、权重驻留和向量阶段流水；DSP 输入输出 pipeline、清零扇出分区和尾块 mask 是从历史硬件版本继承的证据。所有条件项都在报告中标成周期模型，不当作已经在板上跑过的结果。

输入、权重和标定表的 SHA256 见 `data/v9_2026-09-14_010956/trace_manifest.json`。本轮没有新增 LIBERO 成功率实验。
